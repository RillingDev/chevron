"use strict";

/**
 * Store strings to avoid duplicate strings
 */
export const
    _part1 = ": ",
    _factory = "factory",
    _service = "service",
    _error = _part1 + "error in ",
    _isUndefined = " is undefined";
