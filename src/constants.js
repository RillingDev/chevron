"use strict";

/**
 * Store strings to avoid duplicate strings
 */
export const
    _more = ": ",
    _error = "error in ",
    _factory = "factory",
    _service = "service",
    _isUndefined = " is undefined";
