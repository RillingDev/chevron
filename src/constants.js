"use strict";

/**
 * Store strings to avoid duplicate strings
 */
export const
    _more = ": ",
    _is = " is ",
    _factory = "factory",
    _service = "service",
    _error = _more + "error in ",
    _isUndefined = _is + "undefined";
