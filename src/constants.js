"use strict";

/**
 * Store strings to avoid duplicate strings
 */
export const
    _more = ": ",
    _factory = "factory",
    _service = "service",
    _error = _more + "error in ",
    _isUndefined = " is undefined";
